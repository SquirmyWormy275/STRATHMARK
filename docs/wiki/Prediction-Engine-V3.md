# Prediction Engine V3

V3.0.0rc1 is a release candidate that tracks all 232 in-repository
requirements. Repository implementation and audit are complete for this candidate. The
checked-in development-key rehearsal is source-bound and must pass the release verifier.
V2 remains the globally trusted production authority. V3 is not production-eligible,
and V2 is not audit-only.

V3 independently generates transparent formula, hierarchical ML, and blind three-member
LLM-council forecasts from the same sealed evidence. It validates every output,
calibrates and scores settled forecasts, learns context-sensitive credibility, pools
valid distributions, preserves disagreement, and optimizes one legal Mark-3-rebased
sheet for the complete field.

Every valid completion—including both overperformance and underperformance—can update
later capability without labeling motive. One round uses one epoch. Issued sheets and
legal winners remain immutable. Rolling cards move slow inference ahead of the live
call-up path.

The V6, 18-path service contract includes competition lifecycle, frozen snapshot
synchronization, pre-field forecasts, exact-field assembly, typed multi-receipt approval
decisions, and separate official issue acknowledgment. Pre-field receipts contain
signed raw-time seeds but always say `issued_mark=false`; only exact-field assembly
produces marks. The post-format five-run Windows result-to-ready
benchmark recorded a 3.414-second maximum against the 120-second limit.

The documented authority pivot keeps V2 and V3 available as separate eligible choices.
A standalone event selects once; a tournament selects once and all children inherit.
One scope never mixes engines or silently falls back.

The local factory composition/scheduler and bounded production-CNG evaluator entrypoint
exist. Production qualification still needs concrete formula/ML/LLM family executors,
an authenticated local settlement-metric evaluator, provisioned builder/evaluator/signer
OS identities and ACLs, provisioned CNG keys, and exact-source CI evidence. Promotion
remains manual and signed.

The live rolling request binds the promoted council, provider tokens, and deadlines and
seals member and council receipts for exact restart replay. Result settlement accepts the
complete issued roster in one atomic command. Capability, invalidation, scoring,
coverage, weights, readiness, and credibility must all close durably before the
derivation barrier opens; this automation never invents a judge approval decision.
Scheduling alone cannot close that barrier. The live forecast runtime wakes the
same-ledger reaction dispatcher after each durable card publication so the fifth and
final asynchronous component can release readiness without waiting for another race or
restart.

The canonical and complete explanation is
[Prediction Engine V3](../PREDICTION_ENGINE_V3.md).
